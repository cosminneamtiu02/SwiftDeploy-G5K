# Enforce LF line endings to avoid CI vs local differences
* text=auto eol=lf

# Ensure shell scripts keep executable bit and normalized endings
*.sh text eol=lf
bin/** text eol=lf
experiments-runner/** text eol=lf

# Keep this wrapper executable
/.tools/shfmt text eol=lf
